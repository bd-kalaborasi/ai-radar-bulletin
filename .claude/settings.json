{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ -n \"$GH_TOKEN\" ]; then git remote set-url origin \"https://x-access-token:${GH_TOKEN}@github.com/bd-kalaborasi/ai-radar-bulletin.git\"; fi"
          }
        ]
      }
    ]
  }
}
